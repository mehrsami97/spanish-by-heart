# The deployed backend (Render). Vite inlines this at build time, so changing
# it means a rebuild — there is no runtime env on GitHub Pages.
VITE_API_URL=https://spanish-by-heart-backend.onrender.com/api
